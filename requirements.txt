# Runtime dependencies for the Corpus measurement backend.
#
# OpenCV is pinned to the 4.x series: OpenCV 5 changed the return shape of
# cv2.HoughLinesP from (N, 1, 4) to (N, 4). Corpus handles both, but 4.x is
# the series the measurement defaults were tuned against.
opencv-python>=4.8,<5
numpy>=1.24
pillow>=10.0
requests>=2.31
pandas>=2.0
matplotlib>=3.7
