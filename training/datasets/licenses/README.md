# License Evidence

Put reviewed license text or short evidence notes here when a dataset moves
from `needs_review` to `verified`.

Do not store downloaded dataset archives in this folder. Downloaded data belongs
under ignored `data/external/<dataset_id>/`.
